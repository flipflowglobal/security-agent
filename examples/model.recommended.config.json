{
  "_purpose": "Recommended model config for a candle-backed transformer (cargo build --features inference).",
  "_usage": "Copy to <model-dir>/config.json (next to model.safetensors), train or export weights matching these dims, then: security-agent --llm-generate --model <model-dir> <prompt>",
  "n_embd": 64,
  "n_head": 8,
  "n_layer": 6,
  "block_size": 256
}
